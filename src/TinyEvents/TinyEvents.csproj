<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <PackageId>TinyEvents</PackageId>
    <Description>Provider-agnostic transactional outbox with generated event dispatch, durable processing, and explicit database migrations.</Description>
    <PackageTags>events;outbox;source-generator;dotnet;dependency-injection;tinyevents</PackageTags>
  </PropertyGroup>
  <PropertyGroup>
    <PackagedSourceGeneratorPath>$(MSBuildThisFileDirectory)..\TinyEvents.SourceGen\bin\$(Configuration)\netstandard2.0\TinyEvents.SourceGen.dll</PackagedSourceGeneratorPath>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.2" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.2" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\TinyEvents.SourceGen\TinyEvents.SourceGen.csproj" OutputItemType="Analyzer" ReferenceOutputAssembly="false" PrivateAssets="all" />
  </ItemGroup>
  <ItemGroup>
    <None Include="..\..\README.md" Pack="true" PackagePath="\" />
    <None Include="$(PackagedSourceGeneratorPath)" Pack="true" PackagePath="analyzers/dotnet/cs/" Visible="false" Condition="Exists('$(PackagedSourceGeneratorPath)')" />
  </ItemGroup>
  <PropertyGroup>
    <PackageReleaseNotes>
      TinyEvents 1.0.0-beta.1

      Added
      - Explicit previous event-name mappings for durable namespace and type renames.
      - Cleanup storage contract used by the hosted cleanup service.
      - TEV002 compile-time diagnostic for unsupported generic event contracts.

      Hardened
      - Durable event identity, generated dispatch, retry, lease, and failure behavior.
      - Package API, manifest, isolated restore, and runtime compatibility gates.

      Compatibility
      - Evidence-backed pre-1.0 beta with documented at-least-once boundaries.
      - Upgrade every TinyEvents package in the application to beta.1 together.
    </PackageReleaseNotes>
  </PropertyGroup>
</Project>
