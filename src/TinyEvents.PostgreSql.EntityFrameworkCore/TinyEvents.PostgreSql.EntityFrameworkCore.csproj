<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <PackageId>TinyEvents.PostgreSql.EntityFrameworkCore</PackageId>
    <Description>PostgreSQL EF Core provider for transactional outbox storage, worker claiming, and explicit built-in migrations.</Description>
    <PackageTags>tinyevents;outbox;postgresql;postgres;efcore;dotnet;source-generator</PackageTags>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" Version="8.0.11" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\TinyEvents\TinyEvents.csproj" />
  </ItemGroup>
  <ItemGroup>
    <Compile Include="..\Shared\TinyEvents.Migrations\**\*.cs"
             Link="Migrations\Common\%(RecursiveDir)%(Filename)%(Extension)" />
    <Compile Include="..\Shared\TinyEvents.PostgreSql.Migrations\**\*.cs"
             Link="Migrations\PostgreSql\%(RecursiveDir)%(Filename)%(Extension)" />
  </ItemGroup>
  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>
  <PropertyGroup>
    <PackageReleaseNotes>
      TinyEvents.PostgreSql.EntityFrameworkCore 0.1.0-beta.1

      Added
      - Bounded atomic cleanup of eligible processed messages.
      - Migration 002_AddProcessedCleanupIndex for ordered cleanup lookups.

      Hardened
      - PostgreSQL processing, retries, lease recovery, cleanup, migrations, and rolling upgrades against a real database.
      - Current migration history now rejects a missing outbox table instead of accepting inconsistent schema state.

      Compatibility
      - Evidence-backed pre-1.0 beta with caller-owned SaveChangesAsync publishing.
      - Upgrade every TinyEvents package in the application to beta.1 together.
    </PackageReleaseNotes>
  </PropertyGroup>
</Project>
